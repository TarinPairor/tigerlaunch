{
  "name": "conversation-companion",
  "version": "1.0.0",
  "description": "Simple AI conversation companion using OpenAI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Build complete - static files ready' && node -e \"console.log('✓ Server file exists'); console.log('✓ HTML files ready'); console.log('✓ Build successful');\""
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}

